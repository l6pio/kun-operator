apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kun-api
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
